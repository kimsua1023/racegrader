# 오탐률(False Positive Rate) 테스트 케이스 설계

## 1. 목적
RaceGrader가 **실제로는 문제 없는(정상 동기화된) 커널 코드**를 race condition으로
잘못 판정(FAIL/PANIC)하는 비율을 측정하고, 허용 가능한 수준으로 낮춘다.

> 오탐(False Positive) = 정답(정상 동기화) 코드인데 FAIL/PANIC/TIMEOUT이 뜬 경우
> 오탐률 = 오탐 발생 횟수 / 전체 정상 케이스 실행 횟수 × 100%

## 2. 전제 조건 및 현재 상태 (2026-08-11 기준)
- 로그 형식 확정됨 (PR #9, #11 반영):
  - 실패: `[RACEGRADER_FAIL] ASSERT|file:line|SEED|PID|label`
  - 전체 통과: `[RACEGRADER_DONE] SEED|PID`
- ✅ **PR #5(교란 엔진) merge 완료** — 3.2 테스트 실행 가능
  - 실행 커맨드: `make qemu CPUS=2 SEED=<값> CHAOS=1`
  - 메커니즘: CPU별 독립 xorshift32 PRNG, 락 경계(acquire/release) 확률적 지연,
    scheduler() 매 라운드 proc 스캔 순서 Fisher-Yates 셔플
  - 인터럽트 켜진 상태에서만 훅 동작 → 자체 데드락 위험 없음
- `cow_test.c`: 정상(baseline) 버전 6개 체크포인트 포함, 이미 동작 확인됨 → 3.1/3.2 baseline 재료로 사용 가능
- ✅ **`mkfs` 빌드 블로커 해소** (이슈 #17 → PR merge) — `.gitignore` 수정으로 `mkfs.c`가 git에 정상 포함됨, `make qemu` 정상 동작 확인
- ✅ **3.3 블로커 해소** — `CHAOS_PROB_PERCENT`, `CHAOS_MAX_SPIN`이 make 변수로 노출됨 (기본값 30/2000 유지)
- ✅ **`CHAOS=0` 컴파일 에러 해소** (이슈 #24 → PR `fix/chaos-unused-when-disabled` merge) —
  `chaos_rand`/`chaos_spin` 정의를 `#if CHAOS_ENABLED`로 감싸 unused-function 에러 해결.
  3.1a(진짜 무교란 baseline) 실행 및 검증 완료 ([RACEGRADER_DONE] 1|3, 전체 통과)
- ⚠️ **주의: `test/kernel-lock-missing`, `test/kernel-refcount-race`는 결함 버전(정탐/True Positive 테스트 대상)**
  — 오탐 테스트(본 문서)는 정상 동기화 코드(`cow_test.c` 등)만 대상으로 함, 혼동 주의
- N회 반복 실행 자동화는 최우주(CLI)가 담당 예정으로 확정됨 (PR #5 요약 참고)
- [TBD] 오탐률 허용 기준(%), 반복 실행 횟수(N), SEED 값을 몇 개/어떻게 뽑을지(랜덤 N개 vs 고정 범위)

## 3. 테스트 케이스 카테고리

### 3.1 정상 baseline
| 항목 | 내용 |
|---|---|
| 목적 | 최소 기준선 확인 |
| 3.1a — 진짜 무교란 baseline | `make qemu CPUS=2 CHAOS=0` — chaos.c 완전 비활성화, 100% PASS 기대 |
| 3.1b — 교란 켜진 단일 케이스 | `make qemu CPUS=2 CHAOS=1 SEED=1` — 교란 최소 검증용 단일 실행 |
| 대상 | 정상 동기화된 `cow_test.c` |
| 반복 횟수 | 3.1a는 1회로 충분 (교란 없어 결과 고정), 3.1b는 N=[TBD] |
| 판정 | 3.1a가 100% PASS 아니면 CLI/파서 버그로 간주, 최우선 조사 |

### 3.2 정상 코드 + fault injection (핵심 오탐 테스트)
| 항목 | 내용 |
|---|---|
| 목적 | 정상 동기화 코드(`cow_test.c` 등)가 다양한 `SEED` 값 하에서도 `[RACEGRADER_DONE]`을 유지하는지 확인 |
| 방법 | `make qemu CPUS=2 SEED=<값> CHAOS=1`로 SEED를 N개 다른 값으로 바꿔가며 반복 실행, `[RACEGRADER_FAIL]`이 찍히면 오탐 1건 |
| 반복 횟수 / SEED 선택 | N=[TBD], SEED 세트: 랜덤 N개 vs 특정 범위 고정 [TBD] |
| 판정 기준 | 오탐률 [TBD]% 이하 |
| 자동화 | 최우주(CLI)가 N회 반복 실행 담당 예정 |
| 비고 | 이 카테고리가 QA 보고서의 핵심 지표 |

### 3.3 경계 케이스 (TIMEOUT 임계값)
| 항목 | 내용 |
|---|---|
| 목적 | 정상 코드가 fault injection으로 느려졌을 때 TIMEOUT을 오탐으로 뱉지 않는지 확인 |
| 방법 | `make qemu CPUS=2 SEED=<값> CHAOS_MAX_SPIN=<값>` (필요시 `CHAOS_PROB_PERCENT=<값>`도 함께)로 지연 강도를 점진적으로 증가시키며 실행, TIMEOUT 발생 시점 기록 |
| 판정 | 설정된 TIMEOUT 임계값이 정상 실행 편차 대비 여유 있는지 검증 |
| 기본값 | `CHAOS_PROB_PERCENT=30`, `CHAOS_MAX_SPIN=2000` (옵션 미지정 시 기존 동작과 동일) |
| 상태 | ✅ 블로커 해소 (김수아 PR — make 변수로 노출 완료, 기본값 보존 확인됨) — 이제 진행 가능 |

### 3.4 로그 파싱 자체 오탐 (CLI 버그성 오탐)
| 항목 | 내용 |
|---|---|
| 목적 | 커널이 PASS를 뱉었는데 CLI 파서가 잘못 읽어 FAIL로 표시하는 경우 탐지 |
| 방법 | 로그 포맷 경계 케이스(중복 라인, 잘림, 순서 뒤바뀜 등) 인위적으로 넣고 파서 반응 확인 |
| 담당 | 최우주(CLI) 협업 필요 |

## 4. 결과 기록 템플릿
| 시나리오 | SEED / CHAOS | 실행 횟수 | PASS | FAIL(오탐) | 오탐률 | 비고 |
|---|---|---|---|---|---|---|
| cow_test (3.1a 진짜 baseline) | CHAOS=0 | 1 | 1 | 0 | 0% | fix/chaos-unused-when-disabled merge 후 실행, 전체 통과([RACEGRADER_DONE] 1|3) |
| cow_test (3.1b 교란 단일) | CHAOS=1 SEED=1 | | | | | 지금 바로 실행 가능 |
| cow_test (3.2 오탐 테스트) | [합의 후 확정] | N=[TBD] | | | | PR #5 반영 완료 |

## 5. 다음 단계
- [ ] 팀 합의: 오탐률 허용 기준(%), N(반복 횟수), SEED 세트 선정 방식(랜덤 vs 고정 범위) 결정
- [ ] 3.3(TIMEOUT 경계 케이스) 실행 — 지연 강도 점진 증가시키며 TIMEOUT 발생 시점 측정
- [ ] 최우주: `SEED`를 바꿔가며 N회 반복 실행하는 CLI 자동화 진행 상황 확인 (담당은 이미 확정)
- [ ] 김수아: `cow_test.c` 외 다른 정상 동기화 시나리오도 오탐 테스트 대상에 추가할지 확인
       (`test/kernel-lock-missing`, `test/kernel-refcount-race`는 결함 버전이므로 제외)
- [x] 3.1a baseline(`CHAOS=0`) 실행 완료 — 전체 통과, 4번 표 기록됨
- [ ] 결과는 중간보고서 QA 섹션에 포함
